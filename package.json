{
  "private": true,
  "scripts": {
    "test": "run-s lint",
    "lint": "run-s lint:md lint:js",
    "lint:md": "command -v deno >/dev/null && deno fmt ./*.md",
    "lint:js": "standard --global=chrome --global=XMLHttpRequest --fix *.js lib"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "standard": "^17.0.0"
  }
}
